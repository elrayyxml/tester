/**
 * Type definitions entry point.
 *
 * This module aggregates all documentation-only typedefs used by the library.
 * It has no runtime exports; it exists so tooling and consumers can resolve
 * the full contract surface from a single import.
 *
 * @module types
 */

export * from './baileys.js'
export * from './utils.js'